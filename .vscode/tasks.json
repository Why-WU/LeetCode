{
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceRoot}/2.cpp", // 这里是你自己的源文件的名字
                //"${workspaceRoot}/calc.cpp", // 这也是源文件的名字，如果你有多个的话，都列上
                "-o",
                "${workspaceRoot}/a.out"
                
            ],            
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}